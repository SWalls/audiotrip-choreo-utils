import App from './index';

describe('@swalls/audiotrip-choreo-utils', () => {
  describe('App', () => {
    it('returns placeholder name', () => {
      expect(App()).toBe('@swalls/audiotrip-choreo-utils');
    });
  });
});
